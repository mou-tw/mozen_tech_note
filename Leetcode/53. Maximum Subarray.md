


python
```
class Solution:

    def maxSubArray(self, nums: List[int]) -> int:

        maxsum = nums[0]

        curmaxsum = nums[0]

  

        for n in nums[1:] :

            curmaxsum = max(n, curmaxsum + n)

            maxsum    = max(maxsum, curmaxsum)

        return maxsum
```